[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements-backend.txt"

[deploy]
startCommand = "uvicorn backend.api:app --host 0.0.0.0 --port $PORT --workers 2"
healthcheckPath = "/api/config"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
